{
    "$id": "zendesk_fetchUserSegments",
    "$schema": "https://schema.yext.com/config/platform/plugin/v1",
    "globals": {
        "zendeskSubdomain": "${{zendeskSubdomain}}",
        "zendeskUsername": "${{zendeskUsername}}",
        "zendeskPassword": "${{zendeskPassword}}"
    }    
  }